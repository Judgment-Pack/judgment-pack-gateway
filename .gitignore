__pycache__/
*.pyc
store/
*.key
*.seed
go/gateway
# `go test -c` writes the test binary beside the package
*.test

# ecosystem packages under plugins/ build into dist/ and install into node_modules/
plugins/**/node_modules/
plugins/**/dist/
plugins/**/*.tsbuildinfo

# the second implementation installs its typecheck into node_modules/
verify-ts/node_modules/
